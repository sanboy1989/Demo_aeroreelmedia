; Allow larger uploads so hero background videos can be uploaded.
; Most shared hosts (Namecheap etc.) read this file. If your host ignores it,
; set the same values in cPanel "Select PHP Version" > Options, or php.ini.
upload_max_filesize = 128M
post_max_size = 130M
max_execution_time = 300
